import { parse } from '../src';

describe('parse', () => {
  describe('parse simple', () => {
    it('should parse success', () => {
      const message = '不好';
      const result = parse(message);
    });
  });
});
